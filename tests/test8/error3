int method1(int a)
{
    int b,j,c,d;
    int z=0,x=0;
    b = a+10+z+b;
    return b;
}
int method2(int c, int d, int a)
{
    int e;
    e = method1(c);
    e = e + d;
    return e;
}
int mai()
{
    int x=0;
    while(x>800) {
        if (x==0)
            x = x + 1;
        else
            return 1;
    }

    return method2(5,6,7,9,10,x,x+1);
}