int factorial(int x)
{
    if (x <= 1)
        return 1;
    else
        return x * factorial(x - 1);
}

int main()
{
    int n = 3;
    int fact;
    {
        //inner block to test shadowing
        fact = factorial(n + 2);   // should compute factorial(5)
    }
    return fact;
}